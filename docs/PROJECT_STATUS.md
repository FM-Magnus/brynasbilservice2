# Brynäs Bilservice — current project status

Updated 2026-09-18 (Antigravity: canonical design tokens, Level 0 Public Shell, 4-pilot Guide family Second Pass, and Kontakt rebuild — see [the current session log](SESSION_LOG_CURRENT.md) and [`HITL_Temporary_roadmap.md`](../HITL_Temporary_roadmap.md)). The website is a work in progress; Magnus confirmed that **no page copy is final-approved yet**. Routes, page content and image use below were checked against `client/src/`; production behaviour was not checked. See [AGENTS.md](../AGENTS.md) for startup constraints and [AGENT_HANDOFF.md](AGENT_HANDOFF.md) for design history.

**Design system & architecture note:** The site is structured into **7 unique standalone pages** and **2 shared service-page families** (Bilservice family and Guide family), anchored by Level 0 Canonical Design Tokens (`client/src/styles/design-tokens.css`, `--bb-*`), Level 1 canonical shared elements (`client/src/styles/shared-elements.css`, `.bb-*`), and the standalone Public Shell (`PublicHeader`, `PublicFooter`, `GalleryTeaserCard`, `GoogleReviewsCard`, `ContactFormCard`). Legacy `index.css` is strictly frozen and will be deleted when all routes are migrated. Page heroes on migrated pages use the canonical `.bb-hero` layout or `ServiceGuideTemplate.css` with natural mixed-case Archivo 800 display headings and teal accents (`.bb-accent`).

For the Antigravity 2 / Flash 3.8 migration, use the task-specific [Antigravity handoff](ANTIGRAVITY_HANDOFF.md) alongside the repository instructions.

## Purpose and stack

The repository contains Brynäs Bilservice's public website, booking and admin frontend, and an Express API. Magnus directs the product and frontend; Johnny owns the server, database and deployment decisions.

- Active frontend: `client/` — React 18, TypeScript, Vite 4, Tailwind CSS 3, React Router, Archivo/Manrope and canonical `--bb-*` tokens. Build with `npm --prefix client run build`.
- Backend: `server/` — JavaScript, Express 4 and MySQL through `mysql2`. Production is documented as Node 16 on the VPS; its current behaviour was not checked for this dashboard.
- The root React/Vite configuration is orphaned scaffolding. Run and build the frontend from `client/`, not the repository root.
- Git status: branch `redesign/blue-teal-v1`, local and **not pushed**. Always run `git status --short --branch` before relying on this summary — it can drift the moment new local work starts. Never push without Magnus's explicit prior approval.

## Public pages and content work

Every route in this table exists in `client/src/main.tsx`. Text is present in the source, but **all page copy remains work in progress and unapproved**. “Local JPG” means an existing repository image is in use, not that it is the final approved export. `/admin` is a separate protected frontend route.

| Menu area | Route | Current purpose / progress | Copy | Image | Next content task |
| --- | --- | --- | --- | --- | --- |
| Start | `/` | Unique Page: Isolated `LandingPage` island (`.landing-v2__*`): canonical `.bb-hero`, floating `PublicHeader`, cyclic `GoogleReviewsCard` (4.3 / 50 reviews), `ContactFormCard`, reassurance panel, service preview, five-step process, `GalleryTeaserCard`, used-car CTA, and `PublicFooter` | WIP; visual/content reference supplied, not final-approved | `home/landing-v2/landing-sundown-hero.{webp,jpg}` plus existing production workshop, service and vehicle assets | Review visual fidelity and interaction at 1440, 768 and 390 CSS pixels; confirm final landing copy |
| Om oss | `/om-oss` | Unique Page (Step 3 pending rebuild): Transitional workshop, facts, process, Maher portrait card, CTA teaser button to `/galleri`, and handshake hero background overlay | WIP; not approved | `about-hero-bg.{webp,jpg}` hero pair + Maher portrait WebP/JPG | Rebuild as unique standalone page with `PublicHeader` + `PublicFooter` + `GalleryTeaserCard` |
| Galleri | `/galleri` | Unique Page (Step 6 pending rebuild): Image-first workshop viewer with a large selected image, scrollable/drag-enabled thumbnail carousel, previous/next controls and keyboard navigation | WIP; not approved | Eleven selected no-people workshop views; every main image is a 1920px WebP/JPG pair with matching 640px thumbnail pair | Rebuild as unique standalone page mounting `PublicHeader` + `PublicFooter` |
| Biltjänster → Våra tjänster | `/biltjanster` | Unique Page (Complete): Rebuilt as the service-catalog hub (`BiltjansterPage.css`, `.biltjanster-hub__*`), mounts `PublicHeader` + `PublicFooter`. 11 service cards linking to guide & bilservice pages | WIP; not approved | CSS-only hero placeholder; 5 of 11 cards show real photos; Kamrem/Bilbatteri/Bromssystem show topic icons | Provide photos for the remaining 6 cards if any become available |
| Felsökning | `/felsokning` | Bilservice Family (Step 4 pending rebuild): Diagnostics page, OBD focus, symptom picker, pricing. Rebuild on `ServiceReparationerPage.css` (`.bilservice__*`) | WIP; not approved; time-estimate is an unconfirmed branschmässigt riktvärde (`DRAFT GUIDANCE` comment) | 3 real photos (hero, intro, service section) | Rebuild on Bilservice family template with `PublicHeader` + `PublicFooter` |
| Biltjänster → Bilservice | `/service-reparationer#bilservice` | Bilservice Family Owner (Complete): Fully rebuilt on `--bb-*` tokens, `.bb-hero` canonical system, 3 service tiers (Brons/Silver/Guld), process, pricing, mounts `PublicHeader` + `PublicFooter` | WIP; not approved | Real hero photo (`wrench-and-bolt-workbench`) | Confirm copy |
| Biltjänster → Oljebyte | `/oljebyte` | Guide Family (Complete & Second Pass Verified): Rebuilt on `ServiceGuideTemplate.css`, 5 deep-dive topic blocks, `.bb-wrap` containers, mixed-case Archivo 800 H1, mounts `PublicHeader` + `PublicFooter` | WIP; not approved or technically fact-checked | Real hero photo (`oil-drain-under-car`) + funnel photo | Fact-check the technical and service-specific claims with Magnus/workshop |
| Biltjänster → Kamrem | `/kamrem` | Guide Family (Step 5 pending migration): Precision hero, timing-system cards, warning signs. Ready to migrate to `ServiceGuideTemplate.css` | WIP; draft intervals and technical guidance are not fact-checked | Uses local `timing-belt-in-hand` WebP/JPG asset | Migrate to `ServiceGuideTemplate.css`, mount `PublicHeader` + `PublicFooter` |
| Biltjänster → Koppling | `/koppling` | Guide Family (Complete & Second Pass Verified): Rebuilt on `ServiceGuideTemplate.css`, mixed-case Archivo 800 H1, mounts `PublicHeader` + `PublicFooter` | WIP; draft intervals, timing and technical guidance are not fact-checked | No photos exist in `_incoming-assets/` for this topic; placeholder | Fact-check draft technical claims and timing with the workshop |
| Biltjänster → Bromssystem | `/bromssystem` | Guide Family (Complete & Second Pass Verified): Rebuilt on `ServiceGuideTemplate.css`, urgent amber symptom modifier, mixed-case Archivo 800 H1, mounts `PublicHeader` + `PublicFooter` | WIP; draft technical guidance is not fact-checked | No photos exist in `_incoming-assets/` for this topic; placeholder | Fact-check draft intervals, component claims and timing with the workshop |
| Biltjänster → Bilbatteri | `/bilbatteri` | Guide Family (Step 5 pending migration): Battery types (standard, EFB, AGM), benefits, warning signs, service scope. Ready to migrate to `ServiceGuideTemplate.css` | WIP; draft intervals and technical guidance are not fact-checked | 3 real photos (hero, intro, service section) | Migrate to `ServiceGuideTemplate.css`, mount `PublicHeader` + `PublicFooter` |
| Biltjänster → Stötdämpare och fjädrar | `/stodampare-fjadrar` | Guide Family (Step 5 pending migration): Shock absorbers, springs, strut assemblies. Ready to migrate to `ServiceGuideTemplate.css` | WIP; draft intervals and technical guidance are not fact-checked | No photos exist in `_incoming-assets/` for this topic; placeholder | Migrate to `ServiceGuideTemplate.css`, mount `PublicHeader` + `PublicFooter` |
| Biltjänster → Hjullagerbyte | `/hjullagerbyte` | Guide Family (Step 5 pending migration): Wheel bearings, integrated hub units, ABS sensors. Ready to migrate to `ServiceGuideTemplate.css` | WIP; draft intervals and technical guidance are not fact-checked | No photos exist in `_incoming-assets/` for this topic; placeholder | Migrate to `ServiceGuideTemplate.css`, mount `PublicHeader` + `PublicFooter` |
| Biltjänster → Avgassystem | `/avgassystem` | Guide Family (Complete & Second Pass Verified): Rebuilt on `ServiceGuideTemplate.css`, exhaust components, silencers, catalytic converter, mixed-case Archivo 800 H1, mounts `PublicHeader` + `PublicFooter` | WIP; draft intervals and technical guidance are not fact-checked | No photos exist in `_incoming-assets/` for this topic; placeholder | Fact-check draft intervals and technical claims with the workshop |
| Biltjänster → Drivaxel och drivknutar | `/drivaxel-drivknutar` | Guide Family (Step 5 pending migration): Driveshafts, CV joints, rubber boots. Ready to migrate to `ServiceGuideTemplate.css` | WIP; draft intervals and technical guidance are not fact-checked | Real hero photo (`cv-joint-workbench`) | Migrate to `ServiceGuideTemplate.css`, mount `PublicHeader` + `PublicFooter` |
| Biltjänster → Styrning och kulleder | `/styrning-kulleder` | Guide Family (Step 5 pending migration): Ball joints, tie rods, power steering. Ready to migrate to `ServiceGuideTemplate.css` | WIP; draft intervals and technical guidance are not fact-checked | No photos exist in `_incoming-assets/` for this topic; placeholder | Migrate to `ServiceGuideTemplate.css`, mount `PublicHeader` + `PublicFooter` |
| Additional service catalogue | `/tjanster` | Transitional: Existing five-category catalogue | WIP; not approved | Five local service JPGs | Decide its long-term relationship to `/biltjanster` |
| Däck | `/dackservice` | Bilservice Family (Step 4 pending rebuild): Dedicated däckhotell card, legal winter tire dates, 6 service cards & pricing. Rebuild on `ServiceReparationerPage.css` (`.bilservice__*`) | WIP; not approved | `tires-hero-bg.{webp,jpg}` hero pair + 6 real workshop photos | Rebuild on Bilservice family template with `PublicHeader` + `PublicFooter` |
| AC | `/ac-service` | Bilservice Family (Step 4 pending rebuild): AC diagnostics, R134a/R1234yf options, symptom cards, pricing. Rebuild on `ServiceReparationerPage.css` (`.bilservice__*`) | WIP; not approved | `ac-hero-bg.{webp,jpg}` hero pair | Rebuild on Bilservice family template with `PublicHeader` + `PublicFooter` |
| Bärgning | `/bargning` | Unique Page (Step 6 pending rebuild): Towing/transport page, direct-call CTA. Rebuild as unique standalone page | WIP; not approved | `towing-hero-bg.{webp,jpg}` hero pair + local service JPG | Rebuild as unique standalone page with `PublicHeader` + `PublicFooter` |
| Till salu | `/bilar-till-salu` | Unique Page (Step 6 pending rebuild): Vehicle listing and interactive 3-photo gallery. Rebuild as unique standalone page | WIP; not approved | Three local vehicle JPGs | Rebuild as unique standalone page with `PublicHeader` + `PublicFooter` |
| Kontakt | `/kontakt` | Unique Page (Complete): Rebuilt from scratch (`ContactPage.tsx` + `ContactPage.css`, `.kontakt-page__*`), mounts `PublicHeader` + `PublicFooter`, `ContactFormCard`, Google Maps, verified hours | WIP; not approved | No page photo in component | Confirm copy and form handling with the relevant owner |

The desktop Biltjänster dropdown currently orders **Våra tjänster → `/biltjanster`**, **Bilservice → `/service-reparationer#bilservice`**, **Oljebyte → `/oljebyte`**, **Kamrem → `/kamrem`**, **Koppling → `/koppling`**, **Bromssystem → `/bromssystem`**, **Bilbatteri → `/bilbatteri`**, **Stötdämpare och fjädrar → `/stodampare-fjadrar`**, **Hjullagerbyte → `/hjullagerbyte`**, **Avgassystem → `/avgassystem`**, **Drivaxel och drivknutar → `/drivaxel-drivknutar`**, then **Styrning och kulleder → `/styrning-kulleder`**. The separate main navigation also includes **Felsökning → `/felsokning`** after Biltjänster. Mobile Biltjänster links to `/biltjanster`. The old `/tjanster` route remains reachable from page CTAs; its future role has not been decided.

## Copy and image handoff

Pages may retain clearly labelled placeholders while final photography is prepared. New raw photography, graphics and image candidates first belong in [`_incoming-assets/`](../_incoming-assets/), whose [`README`](../_incoming-assets/README.md) defines the subject-based folders, naming and selection process. The inbox deliberately ignores image contents in Git; it is not a runtime asset directory.

As of 2026-09-15 the intake holds unassigned candidates for workshop overviews, biltransport/bärgning, customer interaction, Bilservice, Däck, AC, Drivaxel/drivknutar and blue-toned backgrounds. Ten workshop image pairs and six transport image pairs already have matching JPG thumbnails. These are a reviewed local asset pool, not approval to use them automatically; Magnus must select a page and placement before an agent creates a production export.

Only after an image is selected should it be exported and added to `client/src/assets/images/`. The 2026-09-15 asset pass uses content-based folders (`brand/`, `home/`, `gallery/workshop/`, `people/`, `services/` and `vehicles/`), WebP as primary with JPG fallback when the consuming component supports both, and `-thumb` pairs for image pickers. The `/galleri` viewer uses eleven documented no-people workshop images: each main image is a 1920px production WebP/JPG pair and every thumbnail is a separate 640px pair. Legacy exports are grouped under `client/src/assets/images/archive/` and must never be imported at runtime. See `_incoming-assets/README.md` and `_incoming-assets/ASSET_INVENTORY.md` for the authoritative source-bank workflow and full mapping. Match the actual placement and crop when each asset is integrated; this dashboard sets no universal pixel dimensions. Keep business claims and prices subject to Magnus's approval.

## People and agent roles

| Role | Responsibility for this project |
| --- | --- |
| Magnus | Product decisions, approved copy, visual direction and frontend approval |
| Johnny | Server, database, deployment and production infrastructure |
| Codex | Scoped implementation, source-grounded checks and documentation; Git changes only when expressly requested |
| Claude | Design/content review and planning when tasked, under the same repository boundaries |
| Antigravity | Bounded visual/content iteration with a precise scope, under the same repository boundaries |
| Every agent | Read [AGENTS.md](../AGENTS.md) and [AGENT_HANDOFF.md](AGENT_HANDOFF.md) before edits, preserve uncommitted work, and leave Johnny-owned files untouched without explicit authority |

## Open boundaries

- Automatic deployment is not active in the current repository layout: the workflow file is under `client/.github/workflows/`, not the repository's `.github/workflows/`. Deployment behaviour has not been verified. Magnus and Johnny must agree on a production plan before enabling it.
- `server/.htaccess` and `docs/deployment.md` disagree on the API port and `RewriteBase`. Johnny owns resolution; neither document alone proves the live server configuration.
- The documented database schema differs from the previously inspected live database; local booking submission and production routing remain unverified. `comment_customer` is sent by the client but absent from the server's booking INSERT. Johnny owns backend follow-up.
- `/tjanster` and `/biltjanster` are both defined frontend routes with different content. Magnus must decide whether both remain in the final information architecture.

## Working rhythm

Inspect the current branch, worktree and relevant source; make one bounded change; browser-check UI changes at useful widths; run the client build and `git diff --check` when code changes; update the relevant handoff note. Commit only on an explicit request. Push only on an explicit request after checking the exact branch, changes and destination. Ask Magnus about uncertain facts or approval states before recording them as decisions.
